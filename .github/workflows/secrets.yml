name: Secrets tests

on:
  push:
    branches:
      - main

jobs:
  job1:
    name: test with and reusable
    uses: ./.github/workflows/reusable.yml
    with:
      myinput: '["TOOLS_ACCOUNT_ID", "myawsrole"]'
    secrets: inherit
