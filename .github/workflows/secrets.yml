name: Secrets tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        env:
          MYENVVAR: ${{ secrets.TEST }}
        run: echo $MYENVVAR
